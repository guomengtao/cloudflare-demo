name: WebP Data Pipeline

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch: 

concurrency:
  group: image-processing-sync
  cancel-in-progress: false

jobs:
  run-batch:
    runs-on: ubuntu-latest
    
    # 修改后的权限配置：使用更稳健的 actions 和 contents 权限
    permissions:
      contents: read
      actions: write    # 关键：gh variable set 需要 actions 权限
      checks: write

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Install Dependencies
        run: |
          npm install
          sudo apt-get update && sudo apt-get install -y webp

      - name: 🏃 Run Task Runner
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          B2_ACCESS_KEY_ID: ${{ secrets.B2_ACCESS_KEY_ID }}
          B2_SECRET_ACCESS_KEY: ${{ secrets.B2_SECRET_ACCESS_KEY }}
          B2_BUCKET_NAME: "gudq-missing-assets"
          B2_ENDPOINT: "s3.us-east-005.backblazeb2.com"
          B2_REGION: "us-east-005"
        run: |
          mkdir -p temp
          # 确保路径匹配您的仓库结构：是 task/ 还是 jack/ ？
          # 根据您之前的描述，这里我使用了 task/，请根据实际文件夹名修改
          node task/task-run.js orm-run-webp.js 10

      - name: 🧹 Cleanup Workspace
        if: always()
        run: |
          rm -rf temp/*.jpg
          rm -rf temp/*.webp