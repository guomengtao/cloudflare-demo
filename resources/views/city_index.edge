<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>{{ cityName }} 失踪人口档案列表 - Missing Persons Database</title>
  <style>
    body { font-family: -apple-system, sans-serif; line-height: 1.6; color: #333; max-width: 800px; margin: 0 auto; padding: 20px; }
    h1 { color: #2c5282; border-bottom: 2px solid #e2e8f0; padding-bottom: 10px; }
    .case-list { list-style: none; padding: 0; }
    .case-item { background: white; border: 1px solid #eee; margin-bottom: 10px; padding: 15px; border-radius: 8px; transition: transform 0.2s; }
    .case-item:hover { transform: translateX(5px); border-color: #4299e1; }
    .case-link { text-decoration: none; color: #2b6cb0; font-weight: bold; font-size: 1.1em; }
    .case-meta { font-size: 0.85em; color: #718096; margin-top: 5px; }
  </style>
</head>
<body>
  <nav><a href="/">首页</a> / 城市索引</nav>
  <h1>{{ cityName }} 地区失踪案档案</h1>
  <p>当前城市共有 {{ cases.length }} 条记录</p>

  <ul class="case-list">
    @each(item in cases)
      <li class="case-item">
        <a class="case-link" href="./{{ item.file }}">{{ item.name }}</a>
        <div class="case-meta">失踪日期: {{ item.date || '不详' }}</div>
      </li>
    @endeach
  </ul>
</body>
</html>